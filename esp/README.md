# ESP32 Software (Drone + Remote)

## Commands

```bash
# build, flash and run a specific package
cargo r -r -p "drone"
cargo r -r -p "remote"

# check all packages in workspace
cargo checkall
cargo call

# build all packages in workspace
cargo buildall
cargo ball
```
